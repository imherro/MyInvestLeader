# MyInvestLeader Agent Notes

- 每次研发任务完成后，默认运行相关检查或测试，然后提交并推送到 `origin/main`。
- 同步前必须确认 `.env`、`temp/`、`.pytest_cache/`、`__pycache__/` 等敏感或临时文件没有进入暂存区。
- 只同步研究系统源码、配置、测试、文档和明确需要保留的研究产物。
- 影子仓位相关输出保持只读、比例化，不包含真实资金、股数、下单指令或真实持仓写入。
- 如果测试失败、推送失败或工作区混入无关改动，先说明阻塞点，不做不完整同步。
